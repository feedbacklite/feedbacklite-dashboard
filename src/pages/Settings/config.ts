export interface colors {
  [key: string]: string
}

export const sideBarList: Array<string> = ['general', 'team']

export const sideBarColors: colors = {
  general: 'grey-700',
  team: 'grey-700',
}
